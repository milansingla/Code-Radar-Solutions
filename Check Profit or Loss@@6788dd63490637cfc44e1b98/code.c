#include <stdio.h>

int main() {
    int A,B;
    scanf("%d %d", &a,&b);
    if ((a>b)) {printf("Profit");} 
    else if (a==b){printf("no profit/no loss");}
    else{printf("Loss");}


    return 0;
}